version: '3.7'
services:
  mysql:
    image: mysql
    ports:
      - "3306:3306"
    environment:
      - MYSQL_DATABASE=sns
      - MYSQL_ROOT_PASSWORD=1234
    volumes:
      - ./mysql/init:/docker-entrypoint-initdb.d